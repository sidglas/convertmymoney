<%- include('header') %>

<div class="result">
    <% if (error) { %>
        <%= error %>
    <% } else { %>
        <%= quantidade %> Dólares 
        na cotação <%= cotacao %> 
        é igual a  <%= conversao %> Reais
    <% } %>
</div>
<%- include('footer') %>